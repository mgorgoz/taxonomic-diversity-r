% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_td.R
\name{simulate_td}
\alias{simulate_td}
\title{Simulate Expected AvTD/VarTD Under Random Sampling}
\usage{
simulate_td(
  tax_tree,
  s_range = NULL,
  n_sim = 999L,
  index = c("both", "avtd", "vartd"),
  weights = NULL,
  ci = 0.95,
  seed = NULL,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{tax_tree}{A data frame representing the full regional species
pool taxonomy. First column is species names, subsequent columns
are taxonomic ranks from lowest to highest.}

\item{s_range}{Integer vector of species richness values to
simulate. Default \code{NULL} uses \code{2:S} where S is the total number
of species in \code{tax_tree}.}

\item{n_sim}{Number of random draws per species richness value
(default 999).}

\item{index}{Which index to simulate: \code{"avtd"}, \code{"vartd"}, or
\code{"both"} (default).}

\item{weights}{Optional numeric vector of weights for taxonomic
levels. Passed to \code{\link[=avtd]{avtd()}} and \code{\link[=vartd]{vartd()}}.}

\item{ci}{Confidence interval width (default 0.95).}

\item{seed}{Optional random seed for reproducibility.}

\item{parallel}{Logical. If \code{TRUE}, use parallel processing to speed up
simulations. Default \code{FALSE}.}

\item{n_cores}{Number of CPU cores to use when \code{parallel = TRUE}. Default
\code{NULL} uses \code{parallel::detectCores() - 1}.}
}
\value{
A data frame with class \code{"td_simulation"} containing
columns:
\describe{
\item{s}{Species richness (number of species drawn)}
\item{mean_avtd}{Mean simulated AvTD (if index includes avtd)}
\item{lower_avtd}{Lower CI bound for AvTD}
\item{upper_avtd}{Upper CI bound for AvTD}
\item{mean_vartd}{Mean simulated VarTD (if index includes vartd)}
\item{lower_vartd}{Lower CI bound for VarTD}
\item{upper_vartd}{Upper CI bound for VarTD}
}

Attributes: \code{ci}, \code{index}, \code{n_sim}, \code{pool_size}.
}
\description{
Generates the null distribution of Average Taxonomic Distinctness
(AvTD) and/or Variation in Taxonomic Distinctness (VarTD) by
randomly drawing species subsets from a regional species pool. Used
to construct funnel plots for statistical testing (Clarke & Warwick
1998, 2001).
}
\details{
For each value of S in \code{s_range}, \code{n_sim} random subsets of S
species are drawn (without replacement) from the full species pool
in \code{tax_tree}. AvTD and/or VarTD are computed for each random
subset. The mean and percentile-based confidence limits are
recorded.

The resulting object can be passed to \code{\link[=plot_funnel]{plot_funnel()}} to produce
the classic Clarke & Warwick funnel plot.
}
\examples{
tax <- data.frame(
  Species = paste0("sp", 1:10),
  Genus   = rep(c("G1", "G2", "G3", "G4", "G5"), each = 2),
  Family  = rep(c("F1", "F1", "F2", "F2", "F3"), each = 2),
  Order   = rep(c("O1", "O1", "O2", "O2", "O2"), each = 2),
  stringsAsFactors = FALSE
)
sim <- simulate_td(tax, n_sim = 99, seed = 42)
sim

}
\references{
Clarke, K.R. & Warwick, R.M. (1998). A taxonomic distinctness index
and its statistical properties. Journal of Applied Ecology, 35,
523-531.

Clarke, K.R. & Warwick, R.M. (2001). A further biodiversity index
applicable to species lists: variation in taxonomic distinctness.
Marine Ecology Progress Series, 216, 265-278.
}
\seealso{
\code{\link[=plot_funnel]{plot_funnel()}} for visualisation, \code{\link[=avtd]{avtd()}} and \code{\link[=vartd]{vartd()}}
for the underlying calculations.
}
