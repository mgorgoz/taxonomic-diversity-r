% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rarefaction_taxonomic.R
\name{rarefaction_taxonomic}
\alias{rarefaction_taxonomic}
\title{Taxonomic Diversity Rarefaction}
\usage{
rarefaction_taxonomic(
  community,
  tax_tree,
  index = c("shannon", "simpson", "species", "uTO", "TO", "uTO_plus", "TO_plus", "avtd"),
  steps = 20,
  n_boot = 100,
  ci = 0.95,
  seed = NULL
)
}
\arguments{
\item{community}{A named numeric vector of species abundances (integers).
Names must match the first column of \code{tax_tree}.}

\item{tax_tree}{A data frame with taxonomic hierarchy. First column
is species names, subsequent columns are taxonomic ranks.}

\item{index}{Which index to rarefy. One of \code{"shannon"}, \code{"simpson"},
\code{"uTO"}, \code{"TO"}, \code{"uTO_plus"}, \code{"TO_plus"}, \code{"avtd"}, \code{"species"}
(default: \code{"shannon"}).}

\item{steps}{Number of sample-size steps along the curve (default: 20).}

\item{n_boot}{Number of bootstrap replicates per step (default: 100).}

\item{ci}{Confidence interval width (default: 0.95).}

\item{seed}{Optional random seed for reproducibility (default: NULL).}
}
\value{
A data frame with columns:
\describe{
\item{sample_size}{Number of individuals in the subsample}
\item{mean}{Mean index value across bootstrap replicates}
\item{lower}{Lower bound of the confidence interval}
\item{upper}{Upper bound of the confidence interval}
\item{sd}{Standard deviation across replicates}
}
}
\description{
Computes rarefaction curves for taxonomic diversity indices by
subsampling individuals from the community at increasing sample sizes.
Uses bootstrap resampling to estimate expected diversity and
confidence intervals at each sample size.
}
\details{
The algorithm works as follows:
\enumerate{
\item Expand the abundance vector into an individual-level vector
(e.g., c(sp1=3, sp2=2) becomes c("sp1","sp1","sp1","sp2","sp2")).
\item For each sample size (from min to total N), draw \code{n_boot}
random subsamples without replacement.
\item For each subsample, count species abundances and compute
the chosen diversity index.
\item Return the mean and confidence interval at each step.
}
}
\examples{
comm <- c(sp1 = 10, sp2 = 5, sp3 = 8, sp4 = 2, sp5 = 3)
tax <- data.frame(
  Species = paste0("sp", 1:5),
  Genus   = c("G1", "G1", "G2", "G2", "G3"),
  Family  = c("F1", "F1", "F1", "F2", "F2"),
  stringsAsFactors = FALSE
)
rarefaction_taxonomic(comm, tax, index = "shannon", n_boot = 50)

}
\references{
Gotelli, N.J. & Colwell, R.K. (2001). Quantifying biodiversity:
procedures and pitfalls in the measurement and comparison of species
richness. Ecology Letters, 4, 379-391.

Ozkan, K. (2018). A new proposed measure for estimating taxonomic
diversity. Turkish Journal of Forestry, 19(4), 336-346.
}
\seealso{
\code{\link[=plot_rarefaction]{plot_rarefaction()}} for visualising the rarefaction curve,
\code{\link[=ozkan_pto]{ozkan_pto()}} for full pTO calculation, \code{\link[=shannon]{shannon()}} and \code{\link[=simpson]{simpson()}}
for classical indices, \code{\link[=avtd]{avtd()}} for average taxonomic distinctness.
}
