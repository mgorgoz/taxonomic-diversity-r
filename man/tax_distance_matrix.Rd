% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_distance.R
\name{tax_distance_matrix}
\alias{tax_distance_matrix}
\title{Compute Taxonomic Distance Matrix}
\usage{
tax_distance_matrix(tax_tree, species = NULL, weights = NULL)
}
\arguments{
\item{tax_tree}{A data frame representing the taxonomic hierarchy.
First column must be species names, subsequent columns are
taxonomic ranks from lowest to highest.}

\item{species}{Optional character vector of species names to include.
If NULL, all species in \code{tax_tree} are used.}

\item{weights}{Optional numeric vector of weights for each taxonomic
level. If NULL, equal weights are assigned.}
}
\value{
A symmetric matrix of taxonomic distances between species,
with values ranging from 0 (same species) to 1 (maximum
taxonomic distance).
}
\description{
Calculates pairwise taxonomic distances between species based on
their positions in a taxonomic hierarchy. Distance is computed as
the weighted proportion of taxonomic levels at which two species
diverge.
}
\examples{
tax <- data.frame(
  Species = c("Quercus_robur", "Pinus_nigra", "Fagus_orientalis"),
  Genus = c("Quercus", "Pinus", "Fagus"),
  Family = c("Fagaceae", "Pinaceae", "Fagaceae"),
  Order = c("Fagales", "Pinales", "Fagales"),
  stringsAsFactors = FALSE
)

tax_distance_matrix(tax)

}
