% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot Taxonomic Distance Heatmap}
\usage{
plot_heatmap(
  tax_tree,
  label_size = 3,
  title = NULL,
  low_color = "white",
  high_color = "#B22222"
)
}
\arguments{
\item{tax_tree}{A data frame representing the taxonomic hierarchy,
as produced by \code{\link{build_tax_tree}}.}

\item{label_size}{Numeric value controlling the size of species labels.
Default is 3.}

\item{title}{Optional character string for the plot title.}

\item{low_color}{Color for the lowest distance (most similar).
Default is \code{"white"}.}

\item{high_color}{Color for the highest distance (most distant).
Default is \code{"#B22222"} (firebrick red).}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes the pairwise taxonomic distance matrix as a colored heatmap
using ggplot2. Species are ordered by hierarchical clustering so that
taxonomically similar species appear adjacent.
}
\details{
The heatmap displays the full symmetric distance matrix computed by
\code{\link{tax_distance_matrix}}. The diagonal (self-distance = 0)
appears in the lowest color. Species are reordered using hierarchical
clustering (UPGMA) to reveal taxonomic groupings visually.
}
\examples{
\dontrun{
tax <- build_tax_tree(
  species = c("sp1", "sp2", "sp3", "sp4"),
  Genus   = c("G1", "G1", "G2", "G2"),
  Family  = c("F1", "F1", "F1", "F2")
)
plot_heatmap(tax)
}

}
\seealso{
\code{\link{tax_distance_matrix}}, \code{\link{plot_taxonomic_tree}}
}
